<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.gqfbtc">

    <uses-sdk tools:overrideLibrary="com.sdsmdg.harjot.materialshadows"/>

    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="com.android.launcher.permission.READ_SETTINGS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:name=".base.Application"
        android:allowBackup="true"
        android:icon="@drawable/artboard"
        android:label="@string/app_name"
        android:launchMode="singleTop"
        android:roundIcon="@drawable/artboard"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon, android:theme">
        <activity android:name=".mvp.activity.MainActivity">
        </activity>
        <!-- 登录 -->
        <activity android:name=".mvp.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- 注册 -->
        <activity android:name=".mvp.activity.RegisterActivity">
        </activity>
        <!-- 设置用户名 -->
        <activity android:name=".mvp.activity.SetUserNameActivity">
        </activity>
        <!-- 找回密码 -->
        <activity android:name=".mvp.activity.FindPasswordActivity">
        </activity>
        <!-- 成功 -->
        <activity android:name=".mvp.activity.SuccessActivity">
        </activity>
        <activity android:name=".mvp.activity.BuyBtcActivity">
        </activity>
        <activity android:name=".mvp.activity.BuyAndSellActivity">
        </activity>
    </application>

</manifest>